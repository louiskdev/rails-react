/* @flow */

import React from 'react';
import ResetPasswordContainer from '../../containers/ResetPasswordContainer';

const ResetPasswordScene = (props) => {
  return (
    <ResetPasswordContainer {...props} />
  );
};

export default ResetPasswordScene;
