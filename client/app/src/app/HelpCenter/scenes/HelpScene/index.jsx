/* @flow */
/* eslint-disable max-len */

import React from 'react';
import HelpContainer from '../../containers/HelpContainer';

const HelpScene = (props) => {
  return (
    <HelpContainer {...props} />
  );
};

export default HelpScene;
