/* @flow */
/* eslint-disable max-len */

import React from 'react';
import BubblzContainer from '../../containers/BubblzContainer';

const BubblzScene = (props) => {
  return (
    <BubblzContainer {...props} />
  );
};

export default BubblzScene;
