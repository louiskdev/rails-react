/* @flow */

import React from 'react';
import UserProfileContainer from '../../containers/UserProfileContainer';

const UserProfileScene = (props) => {
  return (
    <UserProfileContainer {...props} />
  );
};

export default UserProfileScene;
